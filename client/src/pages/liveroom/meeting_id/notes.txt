Connect participants and active speaker to real meeting state from your signaling/media layer.
Add screen-share priority logic so shared content auto-promotes to stage.
Persist selected layout mode per user session.

Real participants and active speaker sync: this needs backend signaling infrastructure (room state, participant presence, events),
    plus WebRTC services (STUN/TURN and signaling server).
Screen-share auto-promote: mostly frontend logic once media events exist, but it depends on signaling events from backend
    to keep all clients in sync.
Persist layout mode: no backend required if you store locally (localStorage). Backend is only needed if you want the preference
    to follow the user across devices/accounts.

-------------------------------------

